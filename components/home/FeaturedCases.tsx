import { MysteryBox } from '../../types'; import { CoinAmount } from '../CoinAmount'; import { Button } from '../ui/Button'; import { SectionHeader } from '../ui/SectionHeader';
export const FeaturedCases=({boxes,onOpenBox,onViewAllBoxes}:{boxes:MysteryBox[];onOpenBox:(id:string)=>void;onViewAllBoxes:()=>void})=><section id="featured-cases" className="px-3 py-8 sm:px-6 lg:px-8"><div className="mx-auto max-w-[1250px]"><SectionHeader eyebrow="Curated cases" title="Featured collections" description="Browse cases with visible item pools and odds before you open." action={<Button variant="secondary" onClick={onViewAllBoxes}>View all</Button>}/><div className="grid grid-cols-2 gap-3 md:grid-cols-3 xl:grid-cols-6">{(boxes.length?boxes.slice(0,6):Array.from({length:6}) as MysteryBox[]).map((box,i)=>box?<button key={box.id} onClick={()=>onOpenBox(box.id)} className="group min-h-[190px] rounded-pullz-lg border border-pullz-border-soft bg-pullz-card p-3 text-left shadow-pullz-sm transition duration-200 hover:-translate-y-1 hover:border-pullz-primary/35 hover:shadow-pullz-md"><div className="flex h-28 items-center justify-center"><img src={box.image} alt={`${box.name} case`} className="max-h-28 w-full object-contain transition group-hover:scale-105" loading={i<2?'eager':'lazy'} decoding="async"/></div><p className="mt-3 truncate text-sm font-semibold text-pullz-text">{box.name}</p><CoinAmount amount={Math.round(box.price)} className="mt-1 text-sm font-bold text-pullz-success" iconClassName="h-4 w-4" animated={false}/></button>:<div key={i} className="min-h-[190px] animate-pulse rounded-pullz-lg bg-pullz-card"/>)}</div></div></section>;
