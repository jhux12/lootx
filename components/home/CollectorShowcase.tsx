import React from 'react'; import { Badge } from '../ui/Badge'; import { Button } from '../ui/Button'; import { SectionHeader } from '../ui/SectionHeader';
export type CollectorStory={id:string;username:string;caption:string;mediaUrl:string;timestampLabel?:string;featured?:boolean};
export const CollectorShowcase=({stories,isLoading,onSubmit}:{stories:CollectorStory[];isLoading:boolean;onSubmit:()=>void})=><section className="px-3 py-8 sm:px-6 lg:px-8"><div className="mx-auto max-w-[1250px]"><SectionHeader eyebrow="Collector showcase" title="Recent pulls and deliveries" description="Community submissions from collectors who opened, kept, and shipped real items." action={<Button variant="secondary" onClick={onSubmit}>Submit your pull</Button>}/><div className="flex gap-3 overflow-x-auto pb-2 md:grid md:grid-cols-3 md:overflow-visible">{isLoading?Array.from({length:3}).map((_,i)=><div key={i} className="h-[360px] min-w-[280px] animate-pulse rounded-pullz-lg bg-pullz-card"/>):stories.map(s=><article key={s.id} className="min-w-[280px] overflow-hidden rounded-pullz-lg border border-pullz-border-soft bg-pullz-card shadow-pullz-sm"><div className="aspect-[4/3] bg-pullz-surface"><img src={s.mediaUrl} alt={`${s.username} collector submission`} className="h-full w-full object-cover" loading="lazy" decoding="async"/></div><div className="p-4"><Badge variant={s.featured?'success':'neutral'}>{s.featured?'Delivered':'Customer Pull'}</Badge><h3 className="mt-3 text-base font-semibold text-pullz-text">{s.username}</h3><p className="text-xs text-pullz-text-muted">{s.timestampLabel||'recently'}</p>{s.caption&&<p className="mt-3 text-sm leading-6 text-pullz-text-secondary">{s.caption}</p>}</div></article>)}</div></div></section>;
