import { Trophy } from 'lucide-react'; import { Button } from '../ui/Button'; import { SectionHeader } from '../ui/SectionHeader'; import { Surface } from '../ui/Surface';
export type LeaderboardEntry={id:string;name:string;avatar:string;points:number};
export const HomeLeaderboard=({entries,loaded,onView}:{entries:LeaderboardEntry[];loaded:boolean;onView:()=>void})=><section className="px-3 py-8 sm:px-6 lg:px-8"><div className="mx-auto max-w-[1250px]"><SectionHeader eyebrow="Rewards preview" title="Collector leaderboard" description="A quick look at the current rewards season." action={<Button variant="secondary" onClick={onView}>View leaderboard</Button>}/><Surface variant="elevated" className="p-4">{!loaded?Array.from({length:3}).map((_,i)=><div key={i} className="mb-2 h-16 animate-pulse rounded-pullz-md bg-pullz-overlay"/>):entries.length?entries.map((e,i)=><div key={e.id} className="flex items-center gap-3 border-b border-pullz-border-soft py-3 last:border-0"><span className="grid h-10 w-10 place-items-center rounded-pullz-sm bg-pullz-gold-soft font-bold text-pullz-gold">#{i+1}</span><div className="grid h-10 w-10 place-items-center overflow-hidden rounded-full bg-pullz-overlay text-sm font-bold text-pullz-text">{e.avatar?<img src={e.avatar} alt="" className="h-full w-full object-cover"/>:e.name.charAt(0).toUpperCase()}</div><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold text-pullz-text">{e.name}</p><p className="text-xs text-pullz-text-muted">Current season points</p></div><p className="font-bold tabular-nums text-pullz-text">{e.points.toLocaleString()}</p></div>):<div className="py-8 text-center text-pullz-text-secondary"><Trophy className="mx-auto mb-3 h-8 w-8 text-pullz-gold"/>No active leaderboard entries yet.</div>}</Surface></div></section>;
