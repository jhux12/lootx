import { Box, Sparkles, Truck } from 'lucide-react'; import { SectionHeader } from '../ui/SectionHeader'; import { Surface } from '../ui/Surface';
export const HowItWorks=()=> <section id="how-it-works" className="px-3 py-8 sm:px-6 lg:px-8"><div className="mx-auto max-w-[1250px]"><SectionHeader eyebrow="How it works" title="Collecting, made simple"/><div className="grid gap-3 md:grid-cols-3">{[{icon:Box,title:'Choose a Collection',body:'Browse curated collectible cases.'},{icon:Sparkles,title:'Open Your Pull',body:'Reveal your collectible instantly.'},{icon:Truck,title:'Keep, Ship, or Sell',body:'Choose what happens to the item.'}].map((s,i)=>{const Icon=s.icon;return <Surface key={s.title} className="p-5"><div className="grid h-11 w-11 place-items-center rounded-pullz-sm bg-pullz-primary-soft text-pullz-primary-hover"><Icon className="h-5 w-5"/></div><p className="mt-4 text-sm font-semibold uppercase tracking-[0.12em] text-pullz-text-muted">Step {i+1}</p><h3 className="mt-1 text-lg font-semibold text-pullz-text">{s.title}</h3><p className="mt-2 text-sm leading-6 text-pullz-text-secondary">{s.body}</p></Surface>})}</div></div></section>;
